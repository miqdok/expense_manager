<Page x:Class="ExpenseManager.WPF.Views.TransactionDetailsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:local="clr-namespace:ExpenseManager.WPF"
      xmlns:vm="clr-namespace:ExpenseManager.WPF.ViewModels"
      d:DataContext="{d:DesignInstance Type=vm:TransactionDetailsViewModel}"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      mc:Ignorable="d"
      Title="Transaction Details">
    <Page.Resources>
        <local:AmountColorConverter x:Key="AmountColorConverter" />
        <BooleanToVisibilityConverter x:Key="BoolToVis" />
    </Page.Resources>
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0">
            <Button Content="← Назад" Command="{Binding GoBackCommand}" Padding="8,4" />
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Редагувати" Command="{Binding EditTransactionCommand}" IsEnabled="{Binding IsNotLoading}" Padding="8,4" Margin="0,0,8,0" />
                <Button Content="Видалити" Command="{Binding DeleteTransactionCommand}" IsEnabled="{Binding IsNotLoading}" Padding="8,4" />
            </StackPanel>
        </DockPanel>

        <ProgressBar Grid.Row="1" IsIndeterminate="True" Height="4" Margin="0,8,0,0"
                     Visibility="{Binding IsLoading, Converter={StaticResource BoolToVis}}" />

        <StackPanel Grid.Row="2" Margin="0,16,0,0">
            <TextBlock Text="{Binding Transaction.Id, StringFormat='Id: {0}'}" FontSize="12" Foreground="Gray" />
            <TextBlock Text="{Binding Transaction.WalletId, StringFormat='Гаманець: {0}'}" FontSize="12" Foreground="Gray" />
            <TextBlock Text="{Binding Transaction.Date, StringFormat='Дата: {0:yyyy-MM-dd HH:mm}'}" FontSize="16" Margin="0,8,0,0" />
            <TextBlock Text="{Binding Transaction.Category, StringFormat='Категорія: {0}'}" FontSize="16" Margin="0,4,0,0" />
            <TextBlock Text="{Binding Transaction.Amount, StringFormat='{}{0:N2}'}" FontSize="22" FontWeight="Bold" Margin="0,8,0,0"
                       Foreground="{Binding Transaction.Amount, Converter={StaticResource AmountColorConverter}}" />
            <TextBlock FontSize="14" Margin="0,4,0,0">
                <TextBlock.Style>
                    <Style TargetType="TextBlock">
                        <Setter Property="Text" Value="Тип: Дохід" />
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding Transaction.IsExpense}" Value="True">
                                <Setter Property="Text" Value="Тип: Витрата" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </TextBlock.Style>
            </TextBlock>
            <TextBlock Text="{Binding Transaction.Description, StringFormat='Опис: {0}'}" FontSize="14" Margin="0,8,0,0" TextWrapping="Wrap" />
        </StackPanel>
    </Grid>
</Page>
