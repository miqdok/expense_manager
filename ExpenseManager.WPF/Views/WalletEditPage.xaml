<Page x:Class="ExpenseManager.WPF.Views.WalletEditPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:vm="clr-namespace:ExpenseManager.WPF.ViewModels"
      d:DataContext="{d:DesignInstance Type=vm:WalletEditViewModel}"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      mc:Ignorable="d"
      Title="Wallet Edit">
    <Page.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis" />
    </Page.Resources>
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="{Binding PageTitle}" FontSize="22" FontWeight="Bold" Margin="0,0,0,16" />

        <ProgressBar Grid.Row="1" IsIndeterminate="True" Height="4" Margin="0,0,0,12"
                     Visibility="{Binding IsLoading, Converter={StaticResource BoolToVis}}" />

        <StackPanel Grid.Row="2" Margin="0,0,0,12">
            <TextBlock Text="Назва" FontSize="14" Margin="0,0,0,4" />
            <TextBox Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" FontSize="14" Padding="6" />
        </StackPanel>

        <StackPanel Grid.Row="3" Margin="0,0,0,16">
            <TextBlock Text="Валюта" FontSize="14" Margin="0,0,0,4" />
            <ComboBox ItemsSource="{Binding Currencies}" SelectedItem="{Binding SelectedCurrency}" FontSize="14" Padding="6" />
        </StackPanel>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Left">
            <Button Content="Зберегти" Command="{Binding SaveCommand}" IsEnabled="{Binding IsNotLoading}" Padding="16,8" FontSize="14" Margin="0,0,12,0" />
            <Button Content="Скасувати" Command="{Binding CancelCommand}" IsEnabled="{Binding IsNotLoading}" Padding="16,8" FontSize="14" />
        </StackPanel>
    </Grid>

</Page>
